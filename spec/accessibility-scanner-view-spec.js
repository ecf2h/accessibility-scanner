'use babel';

import AccessibilityScannerView from '../lib/accessibility-scanner-view';

describe('AccessibilityScannerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
